node_modules/
.env
.env.local
.next/
dist/
build/
*.log
.DS_Store
# .squad/ is intentionally NOT ignored — its intake.md and plan files are part of
# the project's history and are meant to be committed.
