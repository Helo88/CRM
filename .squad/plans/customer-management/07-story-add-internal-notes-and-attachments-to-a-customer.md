<!-- squad-kit: generated by anthropic/claude-opus-4-7, at 2026-08-27T23:59:53.132Z, 13 reads / 48.4 KB context, 35 in / 20498 out tokens, scout=on, validation=on, issues=41 -->

# Story 07 — Add internal notes and attachments to a customer

## Prerequisites

- Story 4 completed: `frontend/app/customers/[id]/CustomerProfileForm.tsx`, `frontend/app/customers/[id]/actions.ts`, `frontend/app/customers/[id]/page.tsx`, and the backend `GET /:id` / `PATCH /:id` on `backend/src/routes/customer.routes.ts` render/edit name/email/phone/preferredLanguage today. This story extends that surface — it does not replace it.
- Story 6 completed: `requireAuth` and `requireRole("agent", "admin")` middleware in `backend/src/middleware/auth.ts` are the guardrails re-used here. **Correction: the codebase's auth middleware is named `requireAuth`, not `authenticate` — there is no `authenticate` export anywhere in this repo (confirmed by reading `backend/src/middleware/auth.ts` and every route file that imports it, e.g. `backend/src/routes/customer.routes.ts:4`). Every reference to `authenticate` below means `requireAuth`.**
- **Update — security-admin's permission model has since landed on this branch and supersedes this plan's original "predates security-admin, sticks with plain requireRole" assumption.** `backend/src/routes/customer.routes.ts` already defines and uses a `staffOrDelegatedSubadmin(key)` helper (wrapping `requirePermission` from `backend/src/middleware/auth.ts`, keyed against `backend/src/constants/permissions.ts`'s `customers:manage`) on its existing `GET /` and `POST /` routes, so `agent`/`admin` keep unconditional access while `subadmin` is gated on actually holding `customers:manage`. Every new write route this story adds (`POST /:id/notes`, `POST /:id/attachments`, `PUT /:id/id-document`) reuses that exact same helper — do **not** gate them with plain `requireRole("agent","admin")`. **Also:** `GET /:id` and `PATCH /:id` in that same file were never updated for this model — their inline `isStaff = role === "agent" || role === "admin"` check flatly 403s a `subadmin` today regardless of permissions. This story's Backend Task 4/5 changes fix that in passing, since a delegated subadmin needs to view/edit a profile before adding notes/attachments to it.
- `frontend/components/StepIndicator.tsx` exists and is the stepper visual to reuse. Do **not** author a new stepper.
- Confirm current Node engine before pinning `multer`: run `npm view multer version` and `npm view multer@<version> peerDependencies engines --json` per project dependency-freshness policy in `CLAUDE.md`.

---

## Story Goal

Give staff (agent, admin, or a delegated subadmin holding `customers:manage`) a place — on the existing customer profile — to (1) leave chronological internal notes and (2) upload two kinds of files (a single **ID document** slot that overwrites on re-upload, plus an accumulating **general attachments** list). Notes and the upload controls render only for staff, showing file name, size, uploader name, and upload date; internal notes must **never** be returned to a customer viewing their own profile.

Attachments themselves (the ID document and general attachments) are the one exception to that staff-only rule: a customer viewing their own profile sees them too, but only as a **read-only gallery** of photos/PDFs — no upload controls, no note content, and never another customer's files. To carry that, the customer's own profile page also becomes a 2-step stepper (matching the staff edit form's structure, as agreed), where Step 2 is the read-only gallery instead of the notes/upload panel.

Explicitly not in scope: editing or deleting notes, editing/removing individual general attachments, and any cloud object storage. Storage for this pass is local disk served statically by Express.

---

## Context — Read These Files First

1. `` `backend/src/models/User.ts` `` — read the whole file end-to-end. Confirm:
   - `attachmentSchema` declaration at **line ~58** (`IAttachment` interface just above it — capture the existing fields: `fileName`, `url`, `uploadedBy`, `createdAt`).
   - `internalNoteSchema` declaration at **line ~67** (`IInternalNote` interface just above — fields: `text`, `authorId`, `createdAt`).
   - The two sub-arrays on the user schema: `attachments: [attachmentSchema]` at **line ~94** and `internalNotes: [internalNoteSchema]` at **line ~93**.
   - Whether an `idDocument?: IAttachment` single-slot field exists today (it does not — you will add it).
2. `` `backend/src/routes/customer.routes.ts` `` — read fully, including the already-existing `staffOrDelegatedSubadmin()` helper at the top of the file and its use on the `GET /` and `POST /` routes. Identify the existing `GET /:id`, `PATCH /:id`, and the `toProfileResponse` shaper. `toProfileResponse` gains independent `includeNotes`/`includeAttachments` options (Task 5) so the existing `GET /:id` embeds notes for full staff only and attachments/idDocument for staff-or-self — no new GET routes are added. New **write** endpoints (`POST /:id/notes`, `POST /:id/attachments`, `PUT /:id/id-document`) live in this same router, reusing `staffOrDelegatedSubadmin("customers:manage")` on top of `requireAuth` + `requireRole("agent", "admin", "subadmin")` — the same stack the existing `GET /`/`POST /` routes already use, not a plain `requireRole("agent","admin")`.
3. `` `backend/src/middleware/auth.ts` `` — confirm `requireRole`/`requirePermission` signatures and how they compose with `requireAuth`. Also read `` `backend/src/services/permissions.ts` `` (the `hasPermission(userId, key)` helper), `` `backend/src/constants/permissions.ts` `` (the `customers:manage` key), and `` `backend/src/middleware/README.md` `` (documents the RBAC convention, including the `staffOrDelegatedSubadmin` pattern this story reuses).
4. `` `backend/src/app.ts` `` — read fully. You will add `app.use("/uploads", express.static(path.join(process.cwd(), "uploads")))` before the 404/error middleware and after the existing router mounts. Match the file's existing import ordering.
5. `` `backend/package.json` `` — confirm current dependencies (no `multer` today). New deps to add: `multer` (runtime) and `@types/multer` (dev).
6. `` `backend/.env.example` `` — confirm no storage-related env vars exist; none are being added.
7. `` `backend/tests/routes/customer.routes.test.ts` `` — read to match the test seeding + supertest patterns for the new endpoints, and find the existing "customer self-read" test (Story 4's Test Plan case 3) that already asserts `passwordHash`/`internalNotes`/`attachments` are omitted from `GET /:id` for a self-viewing customer — extend that same test with an `idDocument` assertion rather than adding a new one. **Correction: there is no `GET /me` endpoint anywhere in this backend — `backend/src/routes/me.routes.ts` only has `GET/PATCH /contact` and `GET /email/confirm` (confirmed by reading the file and `backend/tests/routes/me.routes.test.ts`, which tests neither route against a full-profile shape). Every reference below to a customer hitting `GET /me` actually means the customer-self branch of `GET /api/v1/customers/:id` — the only endpoint that returns the profile shape (`toProfileResponse`) at all.**
9. `` `frontend/app/customers/[id]/CustomerProfileForm.tsx` `` — the current single-step form. This becomes **Step 1** unchanged in content, wrapped in a stepper shell.
10. `` `frontend/app/customers/[id]/actions.ts` `` — read fully. The existing `updateProfile` JSON server action stays untouched; new server actions (`addInternalNote`, `uploadAttachments`, `replaceIdDocument`) are added alongside it and use multipart/form-data for the two upload actions.
11. `` `frontend/app/customers/[id]/page.tsx` `` — the profile page. No separate staff-only fetch or `viewerRole` flag is needed: the single `GET /:id` call already returns `internalNotes` only for full staff and `attachments`/`idDocument` for staff-or-self, so the page just passes the response through and `CustomerProfileForm` derives Step 2's mode from its shape (see Frontend Task 1).
12. `` `frontend/components/StepIndicator.tsx` `` — read fully; capture its prop shape (step count, current step index, completed flags) so you wire it correctly.
13. `` `frontend/messages/en.json` `` and `` `frontend/messages/ar.json` `` — add copy for the new step title, note input label/placeholder/submit, ID-document label, general-attachments label, empty-state messages, and error toasts. Match the existing key namespacing (customer profile screen).
14. Sibling precedent: read one of the sibling `.squad/plans/` stories under `customer-management/` for tone (existing plan directories under `.squad/plans/customer-management/`).

---

## Product rules (from story)

- **Current behavior:** `GET /customers/:id` returns name/email/phone/preferredLanguage. There is no notes UI, no attachments UI, no upload endpoint, no static file serving.
- **New behavior:**
  - Staff (agent/admin/subadmin-with-`customers:manage`) viewing a customer profile sees Step 1 (existing fields) and Step 2 (notes list + add-note + ID-document upload + general attachments list + multi-file upload).
  - A customer viewing their own profile also sees a 2-step stepper: Step 1 is identical to today. Step 2 is a **read-only gallery** of their own attachments (ID document + general attachments) — photo thumbnails / PDF tiles with file name and upload date only. No notes, no upload inputs, no uploader/author metadata beyond that.
  - `internalNotes` is **never** returned by any endpoint a customer can call, in any form.
  - `attachments` / `idDocument` **are** returned both to staff and to the profile's own customer (self) — never to a different customer.
  - Uploading a new ID document deletes the previous file from disk and replaces the `idDocument` sub-document. General attachments accumulate; they are not editable/removable in this story.

---

## Backend Tasks

### 1 — Extend the schema

File: `` `backend/src/models/User.ts` ``

- Add a required `size: number` field (bytes) to `attachmentSchema` and to the `IAttachment` interface. Populated server-side from `multer`'s `file.size`; **never** trusted from the client.
- Add a single-slot `idDocument?: IAttachment` field to the user schema and interface, alongside the existing `attachments: [attachmentSchema]` array. This is the chosen shape (rather than a `kind: "id" | "general"` discriminator on the array) because:
  1. The two upload paths have different lifecycles (replace vs. append) and different validation (ID = single file, image/PDF; general = many, any type). A dedicated field makes both the type distinction and the "at most one" invariant load-bearing at the schema level.
  2. Query shape stays trivial: no `$filter` needed to fetch "the ID doc."
  3. It keeps the existing `attachments` array semantics from Story 4 untouched.
- Do **not** touch `internalNoteSchema`. It already carries `text`, `authorId`, `createdAt`.

### 2 — Add `multer` and local upload plumbing

Run in `backend/`:

```
npm view multer version
npm view multer@<latest> peerDependencies engines --json
npm install multer
npm install --save-dev @types/multer
```

Create file: `` `backend/src/middleware/upload.ts`` — a shared `multer` instance:

- Storage: `diskStorage` writing to `path.join(process.cwd(), "uploads", "customers", <customerId>)`. Create the dir on demand (`fs.mkdirSync(..., { recursive: true })`).
- Filename: `<uuid>-<sanitized-original-name>` (sanitize by stripping path separators and control chars).
- `limits: { fileSize: 10 * 1024 * 1024 }` — 10 MB cap per file.
- Export two configured middlewares:
  - `uploadIdDocument` — `.single("file")`, with `fileFilter` that permits only `image/*` and `application/pdf`; reject others with an `Error` whose `.message` maps to a 400.
  - `uploadGeneralAttachments` — `.array("files", 10)` with no `fileFilter` restriction beyond the size cap.

Update file: `` `backend/.gitignore` `` (create if missing) — add `uploads/`. Uploaded files are runtime data.

### 3 — Serve uploads statically

File: `` `backend/src/app.ts` ``

- Import `path` and add `app.use("/uploads", express.static(path.join(process.cwd(), "uploads")))` after the JSON body parsers and before the 404 handler. Stored `IAttachment.url` values will be relative paths like `/uploads/customers/<id>/<file>`.

### 4 — New customer routes

File: `` `backend/src/routes/customer.routes.ts` ``

**Correction: follow the established shape recorded in `.squad/plans/customer-management/00-overview.md` and Story 04's own plan addendum, not a new "sub-resource GET" design.** Both documents are explicit that Story 07 "modifies Story 04's `GET /:id` serializer (`toProfileResponse`) to conditionally include `internalNotes`/`attachments` for staff viewers only" — i.e. staff get notes/attachments **embedded in the existing `GET /:id` response**, not via separate `GET /:id/notes` / `GET /:id/attachments` reads. Drop those two GET routes from this plan; keep only the writes (plus the one read `GET /:id` already needs no new route, just a shaper change):

- **`GET /:id` (existing route, modified, not new):** replace the inline `isStaff = role === "agent" || role === "admin"` with an async check that also admits a `subadmin` holding `customers:manage`:
  ```ts
  const isFullStaff =
    req.user!.role === "admin" ||
    req.user!.role === "agent" ||
    (req.user!.role === "subadmin" && (await hasPermission(req.user!.id, "customers:manage")));
  const isSelf = req.user!.id === String(user._id);
  if (!isFullStaff && !isSelf) { ...403, unchanged... }
  ```
  (`hasPermission` imported from `backend/src/services/permissions.ts` — the same helper `requirePermission`/`staffOrDelegatedSubadmin` already use.) Then call `toProfileResponse(user, { includeNotes: isFullStaff, includeAttachments: true })` (see Task 5) instead of the old `toProfileResponse(user)`. `includeAttachments` is unconditionally `true` at this call site because the handler only ever reaches it for `isFullStaff || isSelf` — both are allowed to see the customer's own files; only `includeNotes` narrows further to staff. When `includeNotes` is true, the response gains hydrated `internalNotes: [{ id, text, createdAt, author: { id, name } }]` (newest first). When `includeAttachments` is true (always, here), the response gains `attachments: [{ id, fileName, size, url, uploadedAt, uploader: { id, name } }]` and `idDocument: {...same shape...} | null`. Resolve every `authorId`/`uploadedBy` → name with a single batched `User.find({ _id: { $in: [...ids] } }, { name: 1 })` lookup per response, not one query per note/attachment. **A self-viewing customer still gets `uploader.name` on their own attachments** (it's a staff member's name, not sensitive to the customer) — only `internalNotes` carries the staff-only restriction.
- **`PATCH /:id` (existing route, modified):** apply the identical `isFullStaff` fix (reuse the same inline check, not a copy that drifts) so a `subadmin` with `customers:manage` can edit a customer's Step 1 fields, matching the roster/create routes' permission story. Its response call becomes `toProfileResponse(user, { includeNotes: isFullStaff, includeAttachments: true })` too, for shape consistency with `GET /:id`.
- `POST /:id/notes` — `requireAuth, requireRole("agent", "admin", "subadmin"), staffOrDelegatedSubadmin("customers:manage")` (reuse the helper already defined at the top of this file — do not redefine it). Body `{ text: string }`. Validate: `text.trim().length >= 1 && length <= 4000`. Push `{ text, authorId: req.user.id, createdAt: new Date() }` onto `internalNotes`. Return the newly hydrated note object (same per-note shape as above).
- `POST /:id/attachments` — `requireAuth, requireRole("agent", "admin", "subadmin"), staffOrDelegatedSubadmin("customers:manage")`, multipart, uses `uploadGeneralAttachments`. For each accepted file, push `{ fileName: file.originalname, url: "/uploads/customers/<id>/<stored-name>", size: file.size, uploadedBy: req.user.id, createdAt: new Date() }` onto `attachments`. Return the hydrated list of newly added entries.
- `PUT /:id/id-document` — `requireAuth, requireRole("agent", "admin", "subadmin"), staffOrDelegatedSubadmin("customers:manage")`, multipart, uses `uploadIdDocument`. If `user.idDocument` exists, resolve its on-disk path from its stored `url` and `fs.promises.unlink` it (log-and-continue on ENOENT so a missing file cannot brick the endpoint). Then set `user.idDocument = { fileName, url, size, uploadedBy, createdAt }` and save. Return the hydrated new value.
- Wrap the two multer-backed routes in an error-adapter so that multer `LIMIT_FILE_SIZE` → 413, `fileFilter` rejection → 400 with a machine-readable code (`"UNSUPPORTED_FILE_TYPE"`), and all other errors → 500 via the existing `errorHandler`.
- A `subadmin` without `customers:manage` gets `403` from `staffOrDelegatedSubadmin` on all three write routes and from the `isFullStaff` gate on `GET`/`PATCH /:id` (for any customer other than themselves — subadmins aren't customers, so this is effectively "always 403" for them without the permission) — the same behavior the existing roster/create routes already have.

### 5 — Split visibility: notes stay staff-only, attachments extend to the profile's own customer

- Change `toProfileResponse`'s signature to `toProfileResponse(user: IUser, opts: { includeNotes: boolean; includeAttachments: boolean })` — **two independent flags, not one `includeInternal`**, because notes and attachments now have different visibility rules.
  - When `includeNotes` is `false`, the returned object must **not** have an `internalNotes` key at all — not `null`/`[]`, simply absent.
  - When `includeAttachments` is `false`, the returned object must **not** have `attachments` or `idDocument` keys at all, same rule.
  - There is no separate `GET /me` in this backend (see Context item 7) — the only callers of `toProfileResponse` are `GET /:id` and `PATCH /:id`, used by staff and self-view alike.
- Both call sites (`GET /:id`, `PATCH /:id`, per Task 4) pass `{ includeNotes: isFullStaff, includeAttachments: true }`. `includeAttachments` is always `true` at these two call sites specifically because both only ever reach that line for an already-authorized viewer (`isFullStaff || isSelf`), and both of those are allowed to see the customer's own files — only `includeNotes` needs the narrower `isFullStaff` check.
- Do not restructure or drop the existing name/email/phone/preferredLanguage/role/createdAt/updatedAt/isActive/ticketHistoryUrl fields — only add the conditional ones.

---

## Frontend Tasks

### 1 — Turn the profile page into a 2-step form for every viewer (Step 2's content differs by role)

File: `` `frontend/app/customers/[id]/page.tsx` ``

- No `viewerRole`/JWT-peek plumbing is needed to decide what Step 2 shows. The existing `GET /:id` call (Backend Task 4/5) already includes `internalNotes` only when the caller is full staff (`isFullStaff`), and always includes `attachments`/`idDocument` for anyone authorized to view the profile at all (staff or the customer themselves — anyone else already got a 403 before this page could render). So: pass the profile response straight into `CustomerProfileForm` and let it derive Step 2's mode from the response shape — `"internalNotes" in profile` means render the staff Internal step; otherwise (the key is absent, but `attachments`/`idDocument` are present) render the read-only gallery step. This is simpler and more robust than a client-side role check, since it can't drift from what the backend actually decided to send.

File: `` `frontend/app/customers/[id]/CustomerProfileForm.tsx` ``

- Introduce a `currentStep` local state (default `0`).
- Always render `StepIndicator` from `frontend/components/StepIndicator.tsx` above the form body with two steps — **every viewer now sees a stepper**, replacing the earlier "customer sees Step 1 only, no stepper" behavior. Step 2's label depends on mode: `"Internal"` for staff, `"Documents"` for a self-viewing customer's read-only gallery.
- **Step 1** body: unchanged for both — the existing name/email/phone/preferredLanguage inputs, submitting via the existing `updateProfile` server action. On successful submit, advance `currentStep` to `1`.
- **Step 2** body branches on whether `profile.internalNotes` is present in the fetched data:
  - **Staff mode** (new subcomponent, e.g. `InternalStep.tsx` in the same folder):
    - **Notes:** chronological list (newest first) rendered as author name + timestamp + text. Below the list, a `<textarea>` and a "Add note" button that invokes `addInternalNote` server action.
    - **ID document:** shows the current file (name, size formatted human-readable, uploader name, upload date, and a link to `IAttachment.url`) if present. Below it, a single `<input type="file" accept="image/*,application/pdf">` and an "Upload ID" button that invokes `replaceIdDocument`. Warn in help text that uploading replaces the current one.
    - **General attachments:** list (name, size, uploader, date, link). Below it, a single `<input type="file" multiple>` and an "Upload files" button that invokes `uploadAttachments`.
  - **Customer self-view mode, read-only** (new subcomponent, e.g. `AttachmentsGalleryStep.tsx` in the same folder):
    - Merge `idDocument` (if present) and `attachments` into one list, newest first.
    - Render as a responsive grid of gallery tiles: an `<img>` thumbnail for entries that look like an image, a PDF tile (linking out to `url`) for entries that look like a PDF, and a generic file tile as a fallback for anything else. Each tile shows only file name and upload date — **no uploader name, no note content, no upload controls.**
    - Empty state: a translated "No documents yet" message when both `idDocument` and `attachments` are empty.
    - **Type inference note:** `IAttachment` doesn't carry a `mimetype`/content-type field (Backend Task 1 only adds `size`). Infer image-vs-PDF-vs-other from the `fileName` extension client-side (`.jpg/.jpeg/.png/.gif/.webp` → image, `.pdf` → PDF, else → generic file tile). Good enough here since the ID-document upload path is already server-side type-restricted to image/PDF; the generic-tile fallback mainly covers general attachments, which stay type-unrestricted on upload. Do not add a `mimetype` field speculatively for this story.

### 2 — New server actions

File: `` `frontend/app/customers/[id]/actions.ts` ``

Add (do not modify `updateProfile`):

- `addInternalNote(customerId: string, formData: FormData)` — POST `/:id/notes` with `{ text }`; revalidate the profile path.
- `uploadAttachments(customerId: string, formData: FormData)` — POST `/:id/attachments` multipart; pass through `FormData` untouched. Revalidate.
- `replaceIdDocument(customerId: string, formData: FormData)` — PUT `/:id/id-document` multipart. Revalidate.

Each returns `{ ok: true, data }` or `{ ok: false, error }` matching the existing action-return shape used by `updateProfile`.

### 3 — Locale strings

Files: `` `frontend/messages/en.json` ``, `` `frontend/messages/ar.json` ``

Add keys for: step titles (`"Profile"`, `"Internal"`, `"Documents"`), notes section title, "Add a note" placeholder, "Add note" button, empty-notes message, "ID document" title/help, "Upload ID" button, "General attachments" title, "Upload files" button, formatted file-size unit strings, error toasts for `UNSUPPORTED_FILE_TYPE` and `LIMIT_FILE_SIZE`, and the read-only gallery step's copy: an empty-gallery message ("No documents yet") and an accessible per-tile label pattern (e.g. "{fileName}, uploaded {date}").

---

## Edge Cases & Failure Modes

- **Customer views their own profile:** the customer-self branch of `GET /:id` (there is no `GET /me`, see Context item 7) must include `attachments` / `idDocument` (rendered client-side as a read-only gallery) but must **never** include `internalNotes`. Enforced in the response shaper (see Backend §5). Regression test: extend the existing customer-self-read test in `backend/tests/routes/customer.routes.test.ts` to assert `internalNotes` is absent while `attachments`/`idDocument` ARE present.
- **Customer views a *different* customer's profile:** unchanged, still 403s before `toProfileResponse` is ever called (the existing `isFullStaff`/`isSelf` gate) — no attachment/gallery data is computed for that request.
- **Subadmin without `customers:manage` opens any customer profile (including their own — subadmins aren't customers, so `isSelf` is never true for them here):** 403 from the `isFullStaff` check on `GET`/`PATCH /:id`, same as the existing `staffOrDelegatedSubadmin` 403 on the roster/create routes and on the three new write routes — consistent behavior across all five staff-facing routes on this resource now, not just the two pre-existing ones.
- **General attachment of an unexpected type in the customer's gallery:** general attachments have no server-side type restriction, so a customer's gallery may include a non-image/PDF file (e.g. `.docx`). The gallery falls back to a generic file tile (name + date, clickable to the raw `url`) rather than failing to render.
- **File type spoofing:** an attacker renames `evil.exe` to `evil.pdf`. The HTML `accept` attribute is UX-only. The `fileFilter` in `uploadIdDocument` inspects `file.mimetype` (multer's parsed value), and only `image/*` / `application/pdf` are accepted. Enforced in `backend/src/middleware/upload.ts`.
- **File larger than 10 MB:** multer emits `LIMIT_FILE_SIZE`. Route adapter returns 413; frontend surfaces a translated toast.
- **ID re-upload with previous file already deleted from disk:** `fs.promises.unlink` on the previous `IAttachment.url` throws `ENOENT`. Catch and log; continue with the save. Never let a missing disk file block a re-upload.
- **Concurrent uploads on the same customer:** multer writes to a per-customer directory, filenames are UUID-prefixed, so name collisions are impossible; two simultaneous `POST /:id/attachments` result in both entries appended in whatever order Mongo receives the `$push`.
- **Very long note text:** server rejects `text.trim().length > 4000` with 400 `TEXT_TOO_LONG`.
- **Empty note:** server rejects `text.trim().length === 0` with 400 `TEXT_REQUIRED`.
- **Unicode / non-ASCII filenames:** preserved in `fileName` (display); stored on-disk path is UUID-prefixed and sanitized (strip `/`, `\`, and control characters) to keep the on-disk representation safe on all platforms.
- **Deleted user as note author:** when hydrating notes, an `authorId` that no longer resolves to a user renders as `"Unknown"` client-side; server returns `author: null`.
- **Static file directory does not exist at boot:** `express.static` will simply 404 requests until the first upload creates the tree. That is acceptable; no boot-time `mkdir` needed.
- **Half-applied schema migration:** existing users have no `idDocument` field. That is fine — it is optional. No migration script needed. If a customer had a pre-existing entry in `attachments` from any earlier ad hoc seed, it will render without a `size`; the shaper must tolerate missing `size` by rendering `"—"`.

---

## Test Plan

Unit / integration tests (backend):

1. `backend/tests/routes/customer.routes.test.ts` — new describe block **"internal notes"**:
   - Agent can `POST /:id/notes`, response contains hydrated author name.
   - Agent's subsequent `GET /:id` includes the new note in `internalNotes`, newest first, with hydrated `author.name`.
   - Customer calling their own `GET /:id` sees **no** `internalNotes` field on the payload.
   - Subadmin holding `customers:manage` can `POST /:id/notes`; subadmin without it gets 403.
   - Empty-text rejected 400; over-4000 rejected 400.
2. Same file — new describe block **"general attachments"**:
   - Agent uploads two files via multipart; response includes both with `fileName`, `size`, `uploader.name`, `uploadedAt`, `url` starting with `/uploads/customers/`.
   - Repeated uploads accumulate.
   - File > 10 MB returns 413.
   - Customer calling their own `GET /:id` sees their own `attachments` present (not omitted).
3. Same file — new describe block **"ID document"**:
   - Agent uploads a PDF; `PUT /:id/id-document` returns the hydrated document.
   - Second upload replaces the first; the first stored file is unlinked (test asserts `fs.existsSync` is false on the previous stored path).
   - Uploading `text/plain` is rejected 400 `UNSUPPORTED_FILE_TYPE`.
   - Customer calling their own `GET /:id` sees their own `idDocument` present.
4. **Correction: there is no `GET /me` endpoint, so this does not touch `me.routes.test.ts` (which only covers `/contact` and `/email/confirm`, confirmed by reading it).** Instead, extend the existing "customer self-read" test in `backend/tests/routes/customer.routes.test.ts` (Story 4's Test Plan case 3, which already asserts `passwordHash`/`internalNotes`/`attachments` are omitted) so it now asserts: `internalNotes` is `undefined`, while `attachments` and `idDocument` (once populated) ARE present — the omission rule narrowed from "all internal fields" to "notes only."
5. `backend/tests/routes/rbac.integration.test.ts`:
   - `POST /:id/notes` and both upload routes 403 for a customer viewer.
   - Same three routes 403 for a subadmin without `customers:manage`, and succeed (200/201) for a subadmin with it.
   - `GET /:id`/`PATCH /:id` on a customer other than self: 403 for a subadmin without `customers:manage`, succeed for one with it (closing the pre-existing gap from Backend Task 4).

Frontend: no automated test framework is configured for the frontend today; smoke via `npm run dev` (this project uses npm — `frontend/package-lock.json` — not pnpm) per Verification Steps below.

---

## Migration / Rollback

- **Schema:** adding an optional `idDocument` and a required `size` on new attachments only. Existing `attachments` documents with no `size` remain readable; UI renders `"—"` for missing size (documented in the shaper). No data migration required.
- **On-disk state:** the `backend/uploads/` directory is runtime data. `.gitignore` excludes it. On rollback, deleting the directory is safe as long as `attachments.url` references are also purged from the DB (or accepted to render broken links). Prefer leaving it in place.
- **Half-applied state:** if the multer install lands but the routes are not yet mounted, uploads simply 404 and existing behavior is unaffected. If the routes are mounted but the static mount is missing, DB rows will point at `/uploads/…` URLs that 404; add the `express.static` mount together with the routes in the same PR.
- **Rollback:** revert the four commits (schema, upload middleware, routes, app.ts static mount) and `npm uninstall multer @types/multer`. No DB drop required — `idDocument` and the `size` field are additive and ignored by prior code.

---

## Verification Steps

1. **Backend builds:** run `npm run build` in `backend/` — expect no TypeScript errors from the new `IAttachment.size` and `idDocument` additions.
2. **Backend tests:** run `npm test` in `backend/` — all new describe blocks pass; existing Story 4/6 tests still green.
3. **Frontend runs:** run `npm run dev` in `frontend/`. Log in as an agent seeded by `backend/scripts/seed-admin.ts`; open a customer profile; verify the stepper shows two steps, Step 1 submits unchanged, Step 2 shows the notes/ID/attachments UI, and a note + a small PDF + two general files can each be added and re-appear after a page reload.
4. **Regression — customer self-view:** log in as the seeded demo customer (`backend/scripts/seed-demo-customer.ts`); open your own profile; verify a 2-step stepper renders, Step 2 shows a read-only gallery of your own ID document + general attachments (thumbnails/PDF tiles, no upload controls, no note content), and the network response for `GET /api/v1/customers/<own-id>` (there is no `GET /me`) includes `attachments`/`idDocument` but has **no** `internalNotes` key.
5. **Regression — permissions:** while logged in as the demo customer, `curl -X POST /customers/<own-id>/notes` — expect 403.
6. **Regression — static:** upload an image via the ID-document control; visit its returned `/uploads/customers/<id>/<file>` URL directly — expect the file to render/download.
7. **Subadmin delegation:** create a subadmin account and grant it `customers:manage` (via the security-admin permissions UI); log in as it; verify it can open a customer profile, add a note, and upload both attachment types — Step 2 renders in staff ("Internal") mode, not the read-only gallery. Then revoke `customers:manage` and verify the same subadmin gets 403 opening that same profile.

---

## Done Criteria

- [ ] `IAttachment` gains a required `size: number` and `User` gains an optional `idDocument: IAttachment` (`backend/src/models/User.ts`).
- [ ] `multer` and `@types/multer` are installed with a version verified fresh against the current Node engine; `backend/uploads/` is git-ignored.
- [ ] `backend/src/middleware/upload.ts` exports the two configured multer middlewares with the 10 MB cap and the `image/*`+`application/pdf` filter for the ID slot.
- [ ] `backend/src/app.ts` serves `/uploads` statically from `backend/uploads/`.
- [ ] `backend/src/routes/customer.routes.ts` exposes `POST /:id/notes`, `POST /:id/attachments`, `PUT /:id/id-document`, all gated by `requireAuth + requireRole("agent", "admin", "subadmin") + staffOrDelegatedSubadmin("customers:manage")` — not plain `requireRole("agent","admin")`; the existing `GET /:id`/`PATCH /:id` are extended so a `subadmin` with `customers:manage` can view/edit too (closing the pre-existing gap), and return uploader/author names resolved server-side via `toProfileResponse`'s independent `includeNotes`/`includeAttachments` flags.
- [ ] The customer-self branch of `GET /:id` (there is no `GET /me` in this backend) never includes `internalNotes`, but **does** include `attachments`/`idDocument` for the profile's own customer.
- [ ] `frontend/app/customers/[id]/CustomerProfileForm.tsx` renders `StepIndicator` and a two-step layout for **every** viewer — staff gets the editable notes/upload "Internal" step, a self-viewing customer gets a read-only "Documents" gallery step (photos/PDFs, no upload controls).
- [ ] `frontend/app/customers/[id]/actions.ts` gains `addInternalNote`, `uploadAttachments`, `replaceIdDocument` server actions using multipart form data for the two uploads.
- [ ] `frontend/messages/en.json` and `frontend/messages/ar.json` include copy for every new label, button, empty state, gallery tile, and error toast.
- [ ] All new backend tests in `backend/tests/routes/customer.routes.test.ts` (including the revised customer-self-read assertion — `internalNotes` absent, `attachments`/`idDocument` present — not `me.routes.test.ts`, which has no relevant endpoint) and the subadmin-delegation + 403 assertions in `backend/tests/routes/rbac.integration.test.ts` pass.
- [ ] Manual verification steps 3–7 all succeed.

**STOP HERE. Report to the user and wait for confirmation before proceeding to Story 08.**
