<!-- squad-kit: generated by anthropic/claude-opus-4-7, at 2026-08-25T14:47:37.233Z, 25 reads / 46.7 KB context, 31 in / 18172 out tokens, scout=on, validation=on, issues=19 -->

# Story 04 — View and edit a customer profile

## Prerequisites

- Auth foundation is in place: `requireAuth` and `requireRole` exist in `backend/src/middleware/auth.ts` (lines 14–44) and `req.user` is typed via `backend/src/types/express.d.ts` (lines 5–14). Stories 1–3 (auth) are assumed **completed** by the executor of this story; if the `/api/v1/auth/register` and `/api/v1/auth/login` handlers in `backend/src/routes/auth.routes.ts` are still stubs, an integration test that logs in as a customer/agent will need to seed a JWT directly rather than call `/login`.
- `User` model already carries every profile field this story exposes — see `backend/src/models/User.ts` (`name` line 26/59, `email` line 27/60, `phone` line 30/63, `preferredLanguage` line 31/64, `createdAt` line 36 + `timestamps: true` line 75, `isActive` line 35/73). **Do not** add a new `CustomerProfile` collection; the header comment at lines 19–24 explicitly defers that split.
- Story 6 (`view-customer-interaction-history`) is a **sibling** story, not a dependency. This story only needs to advertise where the history link points, not build it.
- `.squad/plans/auth/01-story-customer-sign-up.md`, `02-story-login-customer-agent-or-admin.md`, and `03-story-role-based-access-control.md` already exist — this is not the first plan. In particular, **Story 03 already introduces this project's test tooling: Vitest (not Jest) + `supertest`, `backend/vitest.config.ts`, tests under `backend/tests/`, run via `npm test`** (also documented in root `CLAUDE.md`'s "Testing" section). This story's Test Plan builds on that setup rather than installing its own — see Test Plan below.

---

## Story Goal

Deliver a REST surface (and matching TypeScript types) that lets:

1. An **agent or admin** fetch and edit any customer's profile (`name`, `email`, `phone`, `preferredLanguage`).
2. A **customer** fetch and edit **their own** profile only (same editable fields).
3. Any authenticated caller who is authorized to view the profile see `createdAt` (read-only) and a `ticketHistoryUrl` pointer to the Story 6 endpoint shape.

Out of scope: the interaction-history timeline itself (Story 6), internal notes & attachments (Story 7), email-change confirmation flow (Story 5), password change, avatar upload, deletion / deactivation flows, and any frontend UI. This story is **backend-only**.

---

## Context — Read These Files First

1. `backend/src/models/User.ts` — read the full file (78 lines). Confirm the exact field names and shape of `IUser` (lines 25–38) and the schema at lines 57–76. Note that `passwordHash` (line 28/61) **must never** be returned by any endpoint added here.
2. `backend/src/middleware/auth.ts` — read lines 1–44. Note: `requireAuth` writes `req.user = { id, role }` (line 25); `requireRole` returns 403 with the exact error string `"You do not have permission to perform this action"` (line 39). Reuse those semantics.
3. `backend/src/types/express.d.ts` — read lines 1–16. `req.user.id` is a `string` (the JWT `sub`), so compare it to the target user id as a string (`String(user._id) === req.user.id`).
4. `backend/src/app.ts` — read lines 1–33. Follow the existing router-mounting pattern (line 19–21) and the "customers" TODO on lines 22–24; this story is what removes that TODO for customers.
5. `backend/src/routes/ticket.routes.ts` — read lines 1–20. Match the style: `express.Router()`, per-route `requireAuth` + `requireRole` composition, and the 501 stub pattern for routes deferred to sibling stories.
6. `backend/src/routes/conversation.routes.ts` — read lines 1–17. Same style reference.
7. `backend/src/middleware/errorHandler.ts` — read the whole file (small). Ensure thrown / `next(err)`-ed errors flow to a consistent JSON response.
8. `.squad/stories/customer-management/view-customer-interaction-history/intake.md` — skim so the `ticketHistoryUrl` link shape does not contradict Story 6.
9. `.squad/stories/customer-management/maintain-contact-details/intake.md` — skim so the `email` field's edit path here does not preempt the Story 5 confirmation flow (email edits are allowed **but** flagged — see Edge Cases).
10. `CLAUDE.md` — grep for `/api/v1` and `customers` to confirm the versioning + resource-grouping convention used elsewhere.

---

## Product rules (from story)

- **Read (GET):** an agent or admin may read **any user's** profile — customer, agent, or admin (staff routinely need to look up colleagues, not just customers; this is deliberately BROADER than PATCH below, not a typo). A customer may read **only** their own record.
- **Write (PATCH):**
  - An agent or admin may edit the profile of any user whose `role === "customer"` **only** (they do **not** edit other agents/admins via this endpoint — narrower than GET, on purpose: staff can look up colleagues but not modify them here).
  - A customer may edit **only their own** record; ownership is decided by `req.user.id === String(targetUser._id)`, **not** by role.
  - Editable fields for both paths: `name`, `email`, `phone`, `preferredLanguage`. All other fields (`role`, `isActive`, `passwordHash`, `internalNotes`, `attachments`, `isOnline`, `createdAt`, `updatedAt`, `_id`) are **not** editable through this endpoint.
- **Response shape:** never expose `passwordHash`, `internalNotes`, `attachments` (Story 7 owns notes/attachments visibility).
- **Deliberate deviation from `CLAUDE.md`'s "don't re-implement role checks ad hoc inside handlers" convention:** that rule fits plain role-only checks (which `requireRole` already covers everywhere else in this codebase). This story's rules mix role *and* resource ownership (a customer may edit themself regardless of role-based `requireRole`), which `requireRole` cannot express — hence `requireAuth` only, with the role+ownership decision made inside the handler (Tasks 2-3). This is a deliberate, disclosed exception for this specific ownership-based case, not a pattern to copy for simple role-only checks elsewhere.

---

## Backend Tasks

### 1 — Create the customer router

**Create file:** `backend/src/routes/customer.routes.ts`

Follow the shape of `backend/src/routes/ticket.routes.ts` (lines 1–20). Structure:

```ts
import express, { Request, Response, NextFunction } from "express";
import mongoose from "mongoose";
import { requireAuth, requireRole } from "../middleware/auth";
import { User, IUser } from "../models/User";

const router = express.Router();

// Fields safely editable via this endpoint (Story 4).
// role / isActive / passwordHash / internalNotes / attachments are intentionally
// excluded — see USER_STORIES.md customer-management Story 4 and Story 7.
const EDITABLE_FIELDS = ["name", "email", "phone", "preferredLanguage"] as const;
type EditableField = (typeof EDITABLE_FIELDS)[number];

// ...GET /:id and PATCH /:id handlers below
export default router;
```

### 2 — GET `/:id` (fetch a profile)

- Middleware chain: `requireAuth` only. Authorization is done **inside** the handler because rules mix role and ownership (agent/admin can read anyone; customer can read only themself).
- Validate `req.params.id` with `mongoose.isValidObjectId`; return **400** `{ error: "Invalid user id" }` if not.
- Load the user with `User.findById(id).select("-passwordHash -internalNotes -attachments")`. **Do not** chain `.lean()` — it returns a plain object, not an `IUser` (which extends `Document`), and would fail to type-check against `toProfileResponse(user: IUser)` (Task 4) under this project's `strict` TypeScript with no `any` allowed. Without `.lean()`, the result is a proper `IUser`-typed document, matching the serializer's parameter type exactly.
- If not found → **404** `{ error: "Customer not found" }`.
- Authorization decision:
  - If `req.user.role === "agent"` or `"admin"` → allowed.
  - Else if `req.user.id === String(user._id)` → allowed (self-read).
  - Else → **403** using the same error string as `requireRole` (`"You do not have permission to perform this action"`).
- Response body (200):

```json
{
  "id": "<user._id>",
  "name": "…",
  "email": "…",
  "phone": "… | null",
  "role": "customer | agent | admin",
  "preferredLanguage": "en | ar",
  "createdAt": "<ISO string>",
  "updatedAt": "<ISO string>",
  "isActive": true,
  "ticketHistoryUrl": "/api/v1/customers/<id>/history"
}
```

The `ticketHistoryUrl` is a **forward pointer** to Story 6; do not implement the endpoint here. Add an inline comment: `// See customer-management Story 6 (view-customer-interaction-history). URL shape only.`

### 3 — PATCH `/:id` (edit a profile)

- Middleware chain: `requireAuth` only (same reason as GET).
- Validate `req.params.id` with `mongoose.isValidObjectId` → **400** on failure.
- Load the target: `const user = await User.findById(id);` → **404** `{ error: "Customer not found" }` on miss.
- Authorization:
  - `isSelf = req.user.id === String(user._id)`.
  - `isStaff = req.user.role === "agent" || req.user.role === "admin"`.
  - Allowed if `isSelf` **or** (`isStaff` **and** `user.role === "customer"`).
  - Otherwise **403** with the shared error string.
- Body validation:
  - Reject if `req.body` is not a plain object.
  - First check `req.body`'s own keys: any key **not** in `EDITABLE_FIELDS` → **400** `{ error: "Field <name> is not editable" }` (fail-closed; do not silently drop — check before building `updates`, not after). Only once that check passes, build `updates: Partial<Pick<IUser, EditableField>>` by iterating `EDITABLE_FIELDS` and copying the keys present in `req.body`.
  - `name`: must be a non-empty string, trim, max length 200 → **400** `{ error: "name must be a non-empty string" }`.
  - `email`: must be a string matching a minimal regex (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`), lowercased before save (matches the schema `lowercase: true` on line 60). On duplicate (Mongo error code `11000`) → **409** `{ error: "Email already in use" }`.
  - `phone`: string or `null` (to allow clearing). If string, trim; empty string ⇒ store `null`. No format validation here (Story 5 owns richer validation).
  - `preferredLanguage`: must be `"en"` or `"ar"` (matches enum on line 64) → **400** otherwise.
- Apply the updates: `Object.assign(user, updates); await user.save();` (using `.save()` runs schema validators for the enum + unique on `email`).
- Response: **200** with the same body shape as GET (re-serialize using the same helper).

### 4 — Serialization helper

Inside `customer.routes.ts`, add a private `toProfileResponse(user: IUser)` that maps the Mongoose doc to the response body defined above. Ensure `passwordHash`, `internalNotes`, `attachments` are **omitted** — either by projecting them out in the query or by never reading them from the doc when constructing the response.

### 5 — Mount the router

**File:** `backend/src/app.ts`

- Add `import customerRoutes from "./routes/customer.routes";` alongside the existing route imports (currently lines 5–8).
- Insert `app.use("/api/v1/customers", customerRoutes);` after the conversations line (current line 21).
- Remove `customers` from the TODO comment on lines 22–24 (leave the rest of the list intact).

### 6 — No model changes

`backend/src/models/User.ts` **already** has every field this story needs. Do **not** add, rename, or reorder fields. Do not touch the `internalNotes` / `attachments` sub-schemas (Story 7).

---

## Frontend Tasks

**AMENDED 2026-08-25:** same mistake as auth Stories 1-2 — "backend-only, UI is a follow-up story" was written before that follow-up story existed anywhere in the backlog. `CLAUDE.md`'s Conventions now require the UI in the same story. Amending here rather than leaving the backend-only plan as the record of what shipped.

### 4 — Customer profile page

**Files:** `frontend/app/customers/[id]/page.tsx` (Server Component), `frontend/app/customers/[id]/CustomerProfileForm.tsx` (Client Component), `frontend/app/customers/[id]/actions.ts` (Server Action).

- `page.tsx`: `await cookies()` for the session token, redirect to `/` if absent (mirrors `frontend/app/settings/page.tsx`). Server-side `GET /api/v1/customers/[id]` with the bearer token. On 403/404, render a simple "not found or no access" message rather than crashing — a customer hitting another customer's URL gets this legitimately per the backend's own authorization. On 200, render `<CustomerProfileForm profile={...} />`.
- `actions.ts`: one `updateProfile` Server Action, following `frontend/app/login/actions.ts`'s shape — zod schema for `{ name, email, phone, preferredLanguage }` (the backend's full `EDITABLE_FIELDS` set, `backend/src/routes/customer.routes.ts:11`), `PATCH /api/v1/customers/[id]` with the bearer token, return `{ error, fieldErrors }` on failure, `revalidatePath` on success. Email stays in the form — the backend itself allows staff to edit a customer's email immediately (only a customer editing their *own* email is redirected to the confirm-then-apply flow, `customer.routes.ts:106-111`); let the backend's existing 400 surface verbatim rather than duplicating that self/staff distinction client-side.
- `CustomerProfileForm.tsx`: `useActionState` + controlled inputs (see `CLAUDE.md`, "Forms backed by Server Actions" — do not repeat the uncontrolled-input bug found in Stories 1-2). Shows `name`, `email`, `phone`, `preferredLanguage` as editable fields, plus `createdAt` (read-only) and a link built from `ticketHistoryUrl` (renders as a plain link for now — Story 6 doesn't exist yet, so it 404s until then; do not build Story 6's page here).

**Known gap, not this story's job to fix:** there is no `GET /api/v1/customers` list/search endpint and no story anywhere in the 52-story backlog that adds one — an agent can only reach this page via a known customer id (e.g. pasted from a ticket once ticket-management exists, or from `/api/v1/customers` directly via API tooling today). Flagged for the user; do not invent a list/search endpoint speculatively as part of this amendment.

---

## Edge Cases & Failure Modes

- **Malformed ObjectId in `:id`** → 400 `{ error: "Invalid user id" }`. Enforced in both GET and PATCH via `mongoose.isValidObjectId`.
- **Target user does not exist** → 404 `{ error: "Customer not found" }`.
- **Customer tries to read another user** → 403 with the exact string `"You do not have permission to perform this action"` (matches `requireRole` on `backend/src/middleware/auth.ts:39`).
- **Agent/admin PATCHes another agent/admin** → 403. Only `user.role === "customer"` targets are editable by staff via this endpoint (privilege-escalation guard).
- **Customer PATCHes another customer** → 403 (ownership check fails).
- **Unknown or non-editable field in body** (e.g. `role`, `isActive`, `passwordHash`, `_id`) → 400 with the field name; do **not** apply partially. Enforced in the field-loop in Task 3.
- **Duplicate email** → Mongo throws `E11000` on save because of the `unique: true` on `backend/src/models/User.ts:60`; translate to 409 `{ error: "Email already in use" }`.
- **Email change by a customer** → allowed here (returns updated record), but note in a code comment that Story 5 will introduce a confirmation flow that supersedes the immediate change. Do not implement the confirmation here; the executor of Story 5 will refactor this path.
- **`phone: null` (clear phone)** → treat as valid; store `null`. `phone: ""` → coerce to `null`.
- **Empty body `PATCH`** (`{}`) → 400 `{ error: "No editable fields provided" }`. Prevents no-op writes that would still bump `updatedAt`.
- **Unicode / RTL names** → allowed unchanged; do not normalize. `preferredLanguage: "ar"` is a valid enum value on schema line 64.
- **Concurrent edits** → last-write-wins; `updatedAt` reflects the winner. Documented; not mitigated.
- **JWT `sub` refers to a user that was deleted** → GET/PATCH still authenticates but the ownership branch fails; caller falls through to the role branch. Acceptable; the deletion story is not in scope.
- **`isActive: false` customer** → still readable and editable via this endpoint (deactivation UX is a separate concern). Do not filter them out.

---

## Test Plan

This project's test runner is **Vitest** (introduced by Story 03 — see `.squad/plans/auth/03-story-role-based-access-control.md` and `CLAUDE.md`'s "Testing" section), not Jest, and tests live under `backend/tests/`, not colocated `__tests__/` folders. If Story 03 has landed by the time this story executes, `backend/vitest.config.ts` and `backend/tests/setup.ts` already exist — do not reinstall or reconfigure Vitest, just add to what's there. If it hasn't landed yet, set it up the same way Story 03's plan does (Vitest + supertest, `backend/vitest.config.ts`, `backend/tests/setup.ts`) rather than improvising a different location or a Jest-based one.

**Option A — Add tests (preferred).** Create `backend/tests/routes/customer.routes.test.ts` covering:

1. **Unit — auth gate:** GET `/api/v1/customers/:id` without `Authorization` → 401 with `"Missing or invalid Authorization header"` (matches `auth.ts:19`).
2. **Unit — invalid id:** GET with `:id = "not-an-objectid"` → 400.
3. **Integration — customer self-read:** seed a customer, sign a JWT with `sub = user._id.toString()`, GET their own id → 200, response omits `passwordHash`, `internalNotes`, `attachments`; includes `ticketHistoryUrl`.
4. **Integration — customer reads another customer:** → 403.
5. **Integration — agent reads any customer:** → 200.
6. **Integration — admin reads agent:** → 200 (staff can read anyone).
7. **Integration — customer PATCHes self (`name`, `phone`, `preferredLanguage: "ar"`):** → 200, DB reflects change, `updatedAt` advanced.
8. **Integration — customer PATCHes another customer:** → 403.
9. **Integration — agent PATCHes customer:** → 200.
10. **Integration — agent PATCHes another agent:** → 403 (staff-on-staff blocked).
11. **Integration — PATCH with `role: "admin"` in body:** → 400 (`role` is not in `EDITABLE_FIELDS`).
12. **Integration — PATCH with duplicate email:** → 409.
13. **Integration — PATCH with empty body:** → 400 (`"No editable fields provided"`).
14. **Integration — PATCH `phone: null`:** → 200, DB shows `phone === null`.

**Option B — Manual verification via `curl`.** Add a `docs/`-style comment at the top of `customer.routes.ts` with example requests for cases 1, 3, 4, 7, 8, 11, 12, 13 above, and document them in the PR description. `cd backend && npm run typecheck` must still pass.

Whichever option is chosen, `cd backend && npm run typecheck` (or the repo's equivalent) must pass with **no new `any`** in the route file — use `Partial<Pick<IUser, EditableField>>` for the update payload.

---

## Verification Steps

1. **Backend builds:** in `backend/`, run `npm run typecheck` — must exit 0.
2. **Backend boots:** in `backend/`, run `npm run dev`; hit `GET http://localhost:<port>/api/v1/health` (existing route from `backend/src/routes/health.routes.ts`) to confirm the app still starts after adding the new router.
3. **Route smoke (unauthenticated):** `curl -i http://localhost:<port>/api/v1/customers/000000000000000000000000` → expect **401**.
4. **Route smoke (authenticated as seeded customer):** with a hand-issued JWT, GET the customer's own id → **200** and confirm the JSON keys match the shape in Task 2.
5. **Route smoke (PATCH):** PATCH `{ "name": "New Name" }` as the same customer → **200**; re-GET shows the updated name.
6. **Regression:** existing routes (`/api/v1/auth/*`, `/api/v1/tickets`, `/api/v1/conversations`) continue to respond with their prior status codes; the 404 fallback in `backend/src/app.ts:26–28` still catches unknown paths.
7. **Tests (if Option A adopted):** `npm test` in `backend/` — all cases in Test Plan pass.

---

## Done Criteria

- [x] `backend/src/routes/customer.routes.ts` exists and implements GET `/:id` and PATCH `/:id` per Tasks 2–4.
- [x] `backend/src/app.ts` mounts the router at `/api/v1/customers` and the `customers` TODO is removed from lines 22–24.
- [x] Response bodies never contain `passwordHash`, `internalNotes`, or `attachments`.
- [x] Agent/admin can GET and PATCH any `role === "customer"` user; PATCH against a non-customer target returns 403.
- [x] A customer can GET and PATCH **only** their own record (verified by both a self-success and a cross-user 403 test).
- [x] `EDITABLE_FIELDS` is exactly `["name", "email", "phone", "preferredLanguage"]`; any other body key returns 400.
- [x] Duplicate-email PATCH returns 409, not 500.
- [x] Response includes `ticketHistoryUrl: "/api/v1/customers/<id>/history"` with a code comment pointing to Story 6.
- [x] `npm run typecheck` in `backend/` passes with no new `any` usages in the new file.
- [x] `backend/src/models/User.ts` is unchanged. *(True for Story 4's own scope — Story 5 below is what later extended it with `pendingEmail`/`emailConfirmToken` fields.)*

**STOP HERE. Report to the user and wait for confirmation before proceeding to Story 05.**

---

## Addendum (2026-08-27): Staff customer roster

This story's plan explicitly flagged, and deliberately did not build, a list/search endpoint:

> **Known gap, not this story's job to fix:** there is no `GET /api/v1/customers` list/search endpoint and no story anywhere in the 52-story backlog that adds one... Flagged for the user; do not invent a list/search endpoint speculatively as part of this amendment.

The user has since directed this to be built. As with the refresh-token addendum in the auth/02 plan, this is net-new scope with no corresponding entry in `USER_STORIES.md` — whoever owns that file's prose should add one if it belongs there; not done here per this repo's convention that story text is user-authored.

### What was built

- **Backend:** `GET /api/v1/customers` (`backend/src/routes/customer.routes.ts`) — `requireAuth` + `requireRole("agent", "admin")`, paginated (`?page=&limit=`, default 20/page, capped at 100), filtered to `role: "customer"` only (this is a customer roster, not the agent/admin account list Story 45 will own separately). Response fields: `id`, `name`, `email`, `phone`, `isActive`, `createdAt` — no `passwordHash`/`internalNotes`/`attachments`, same discipline as the existing GET `/:id`.
- **Frontend:** `frontend/app/customers/page.tsx` — table (name/email/phone/status/joined/history), Previous/Next pagination, a "no access" state for a non-staff visitor who navigates there directly (rather than a raw 403 or crash). `frontend/components/StaffSidebar.tsx` — a left nav reusing the `--sidebar-*` design tokens that were reserved in `globals.css` but unused until this. A "Customers" link appears in `SiteHeader`'s nav, staff-only (role read from an unverified JWT peek, purely a UI decision — the backend `requireRole` above is the actual authorization boundary).
- **Deliberately excluded:** per-customer ticket counts / activity summaries. Ticket-management is still unbuilt (501 stubs), so that data doesn't exist yet. Recommended sequencing, discussed with the user: extend *this same* roster page with a ticket-count column once ticket-management ships, rather than building a separate customer-facing list — the "View history" link already on both this table and the single-customer profile page is wired to the same not-yet-real `GET /api/v1/customers/:id/history` shape (Story 6), so it's consistent, if still non-functional, in both places.
- **Also removed** (2026-08-27, user request): the "Preferred language" field on the single-customer edit form (`CustomerProfileForm.tsx`) — now redundant with the real language switcher in `UserMenu`. The backend still accepts `preferredLanguage` in `EDITABLE_FIELDS` (line 12) and the model field is untouched; only this form's UI was changed. `en.json`'s `languageEnglish`/`languageArabic`/`preferredLanguage` keys are now unused by any component — left in place rather than removed speculatively, in case a future story revives a language-preference field elsewhere.

### Done Criteria (addendum)

- [x] `GET /api/v1/customers` returns 403 for a `role: "customer"` caller, 200 with paginated data for `agent`/`admin` — verified via real login + curl for both roles, not just code inspection.
- [x] Response never includes `passwordHash`/`internalNotes`/`attachments` (same field allowlist discipline as GET `/:id`).
- [x] Frontend table renders live data, pagination controls work, "Customers" nav link is staff-only, and a customer navigating to `/customers` directly sees the "no access" state rather than a crash — all verified via Playwright screenshots in both roles.
- [x] `npm run build` (frontend) and `npm run typecheck` + `npm test` (backend, 58/58) all pass clean.
